# The daemon reads evdev through the input group. Do not grant a seat-wide ACL:
# other programs in the desktop session would gain the same raw input access.
KERNEL=="event*", SUBSYSTEM=="input", GROUP="input", MODE="0660"
